{
  "name": "@shared/contracts",
  "version": "0.0.1",
  "description": "Typed API contracts (domain types + shared GraphQL client) shared by all micro-frontends.",
  "private": false,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "build": "node -e \"console.log('@shared/contracts is source-only (no build step).')\""
  },
  "dependencies": {
    "@apollo/client": "^4.2.12"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "9.39.5",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.35.0"
  }
}
