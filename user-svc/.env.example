# user-svc environment (copy to .env for local dev)
SERVICE_NAME="user-svc"
PORT=4003
NODE_ENV="development"

# PostgreSQL connection (shared Prisma schema lives in @server/shared)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/microfrontend?schema=public"
