name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: Lint, typecheck, test, build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout cart-svc
        uses: actions/checkout@v4
        with:
          repository: jrumandal/cart-svc
          path: cart-svc

      - name: Checkout server-shared (sibling workspace)
        uses: actions/checkout@v4
        with:
          repository: jrumandal/server-shared
          path: server-shared

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.24.0
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Link sibling into a workspace
        run: |
          printf 'packages:\n  - "cart-svc"\n  - "server-shared"\n' > pnpm-workspace.yaml
          printf '{\n  "name": "mf-workspace",\n  "private": true\n}\n' > package.json

      - name: Install
        run: pnpm install

      - name: Build server-shared (prisma generate + tsc)
        working-directory: server-shared
        run: pnpm prisma:generate && pnpm build

      - name: Lint
        working-directory: cart-svc
        run: pnpm lint

      - name: Typecheck
        working-directory: cart-svc
        run: pnpm typecheck

      - name: Test
        working-directory: cart-svc
        run: pnpm test

      - name: Build
        working-directory: cart-svc
        run: pnpm build
